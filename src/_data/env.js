module.exports = {
    // This makes the environment available to templates
    // Returns either "production" or "development"
    // So you can check for the environment and do things in either production or development

    "env": process.env.ELEVENTY_ENV
    
}